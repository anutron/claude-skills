## Plan Archiving

**When a plan is approved and implemented** in a project that uses spec-driven development (has a `.specs` file), copy it to `specs/plans/` within that project with a descriptive filename (e.g., `gmail-mcp-server.md`, `time-survey-sync.md`). This preserves architectural decisions and context for future reference.
